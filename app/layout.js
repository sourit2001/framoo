export const metadata = {
  title: 'AI Image Fusion',
  description: 'Create amazing fused images with AI',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
